<section class="shrink">
  <div class="animate__animated animate__fadeInUp animate__delay-1s restaurant">
    <div class="content-info">
    <div class="info">
      <h2>{{values.name}}</h2>
      <h3>{{values.type}}</h3>
      <h3>Price: {{values.price}}</h3>
      <p>{{values.address}}</p>
    </div>
    <div class="description">
    <h3>Description</h3>
      <p>{{values.description}}</p>
      <p>{{values.username}}</p>
    </div>
  </div>
  </div>
</section>

<section class="animate__animated animate__fadeInUp animate__delay-2s form-flex">
  <form class="form-box review-frame">
    <label><h2>Add a Review<img src="../public/css/images/review.png" class="thumbsup"></h2></label>

    <textarea name="comment-body" placeholder="Leave your review here"></textarea>

    <button type="submit" class="form-btn">Submit</button>
  </form>
</section>
    
  <section>
    <p>this is my favortie restaurant</p>
    <p>posted on mm/dd/yyyy by username</p>
  </section>

